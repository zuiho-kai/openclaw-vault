{
  "id": "vault",
  "kind": "skill",
  "name": "Vault",
  "version": "1.1.2",
  "description": "安全的本地密码存储工具（AES-256-GCM 加密）",
  "requiredEnv": ["VAULT_MASTER_KEY"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "masterKey": {
        "type": "string",
        "description": "主加密密钥（也可使用 VAULT_MASTER_KEY 环境变量）"
      },
      "storageFile": {
        "type": "string",
        "default": ".vault/passwords.json",
        "description": "密码存储文件路径（相对于用户目录）"
      }
    }
  },
  "uiHints": {
    "masterKey": {
      "label": "Master Key",
      "placeholder": "your-secure-master-key",
      "help": "Master encryption key (can also use VAULT_MASTER_KEY env var)",
      "secret": true
    },
    "storageFile": {
      "label": "Storage File",
      "placeholder": ".vault/passwords.json",
      "advanced": true,
      "help": "Password storage file path relative to home directory"
    }
  }
}
