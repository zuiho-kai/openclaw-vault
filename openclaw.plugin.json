{
  "id": "vault",
  "kind": "skill",
  "name": "Vault",
  "version": "1.0.1",
  "description": "简单的本地密码存储工具（明文存储，仅用于非关键凭据）",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "storageFile": {
        "type": "string",
        "default": ".vault/passwords.json",
        "description": "密码存储文件路径（相对于用户目录）"
      }
    }
  },
  "uiHints": {
    "storageFile": {
      "label": "Storage File",
      "placeholder": ".vault/passwords.json",
      "advanced": true,
      "help": "Password storage file path relative to home directory"
    }
  }
}
